# Examples

This directory will contain:

- `messy.csv` — real-world problematic input
- `normalized.csv` — deterministic output
- `report.json` — machine-readable explanation

These examples are part of the contract.
